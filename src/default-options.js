'use strict';

module.exports = Object.freeze({
  as: 'stream',
  followRedirects: true,
  successOnly: true,
  method: 'GET',
  body: null,
  telemetry: null,
  headers: Object.freeze({}),
  query: {},
});
